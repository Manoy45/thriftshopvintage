{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié - Thrift Shop Vintage
{% endblock %}

{% block content %}
	<div class="block-small">
    <h1 class="h2 mb-3 font-weight-normal text-center">Réinitialiser mot de passe</h1>
    <p class="text-center">Veuillez saisir et confirmer votre nouveau mot de passe :</p>
    {% for message in app.flashes('notice') %}
        <div class="alert alert-info">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('notice') %}
        <div class="alert alert-info">{{ message }}</div>
    {% endfor %}
		{{ form(form) }}
	</div>
{% endblock %}
